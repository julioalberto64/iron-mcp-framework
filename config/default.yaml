server:
  name: mcp-node-framework-starter
  version: 0.1.0

transport: http

http:
  host: 127.0.0.1
  port: 3000
  path: /mcp

logging:
  level: info

api:
  baseUrl: https://httpbin.org
  timeoutMs: 10000

auth:
  type: bearer-env
  token: ""
  apiKeyHeader: x-api-key
  apiKey: ""
